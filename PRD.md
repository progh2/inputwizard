# InputWizard — Product Requirements Document

## 1. 제품 개요

**InputWizard**는 키보드, 마우스 스크롤, 단축키를 사용하기 어려운 상황(입력 장치 고장, 접근성 보조, 특수 환경 등)에서 보조 입력 수단을 제공하는 플로팅 버튼 유틸리티다.

배경 없이 화면 위에 떠 있는 최소한의 버튼 모음으로 구성되며, 자유롭게 화면 어디로든 드래그해서 위치를 바꿀 수 있다.

---

## 2. 목표

- 마우스 스크롤·단축키·한영키를 대체할 수 있는 보조 버튼 제공
- OS(Windows / macOS / Linux) 를 가리지 않고 동작
- 항상 최상단(always-on-top)에 떠 있으면서도 화면을 최소한으로 가림
- 파이썬 기반으로 소스 공개 및 직접 빌드 가능

---

## 3. 기능 요구사항

### 3.1 창 레이아웃

| 영역 | 구성 |
|------|------|
| 좌측 열 | 세로 버튼 3개 (한/영 전환 · 복사 · 붙여넣기) |
| 우측 열 | 위 스크롤 버튼 + 아래 스크롤 버튼 |

- 창은 프레임 없음(frameless), 배경 투명, always-on-top
- 클릭&드래그로 창 위치 자유 이동
- 마지막 위치를 설정 파일에 저장하여 재실행 시 복원

### 3.2 버튼 기능 상세

#### [L1] 한/영 전환 버튼
- **Windows**: `hangul` 가상 키 이벤트 전송 (VK code `0x15`)
- **macOS**: 입력 소스 전환 단축키 `Ctrl+Space` 또는 한/영 키 시뮬레이션
- **Linux (X11)**: `xdotool key hangul` 호출
- **Linux (Wayland)**: `ydotool key 0x90` 또는 IBus 전환 API 호출

#### [L2] 복사 버튼
- 현재 포커스된 창의 선택 영역을 클립보드에 복사
- OS별 단축키 시뮬레이션: `Ctrl+C` (Win/Linux) · `Cmd+C` (macOS)

#### [L3] 붙여넣기 버튼
- 현재 커서 위치에 클립보드 내용 붙여넣기
- OS별 단축키 시뮬레이션: `Ctrl+V` (Win/Linux) · `Cmd+V` (macOS)

#### [R1] 위 스크롤 버튼
- 현재 포커스된 요소에 마우스 휠 위 방향 입력 전송
- 1회 클릭 = 기본 3틱(설정 가능)
- 꾹 누르면 연속 스크롤 (딜레이 500ms 후 시작, 이후 100ms 간격)

#### [R2] 아래 스크롤 버튼
- 현재 포커스된 요소에 마우스 휠 아래 방향 입력 전송
- 1회 클릭 = 기본 3틱(설정 가능)
- 꾹 누르면 연속 스크롤 (딜레이 500ms 후 시작, 이후 100ms 간격)

### 3.3 버튼 표시

| 버튼 | 이모지 |
|------|--------|
| 한/영 전환 | 韓 |
| 복사 | 📋 |
| 붙여넣기 | 📌 |
| 위 스크롤 | ↑ |
| 아래 스크롤 | ↓ |

### 3.4 UX 요구사항

- 버튼 클릭 시 포커스가 InputWizard 창으로 이동하지 않아야 함 (포커스 스틸 방지)
- 버튼에 툴팁 제공 (한/영, 복사, 붙여넣기, ↑스크롤, ↓스크롤)
- 우클릭 컨텍스트 메뉴: 스크롤 강도 설정 · 항상 위 토글 · 종료

---

## 4. 비기능 요구사항

| 항목 | 기준 |
|------|------|
| 지원 OS | Windows 10+, macOS 12+, Ubuntu 22.04+ |
| Python | 3.10+ |
| GUI 프레임워크 | PySide6 |
| 전역 입력 라이브러리 | `pynput` (키보드/마우스 이벤트 주입) |
| 설정 저장 | JSON (플랫폼 표준 config 경로) |
| 실행 파일 배포 | PyInstaller (선택적) |
| 메모리 사용량 | 상시 실행 기준 50 MB 이하 목표 |

---

## 5. 마일스톤

| 버전 | 목표 | 주요 완료 조건 |
|------|------|--------------|
| v0.1.0 | 기본 UI | 투명 프레임리스 창, 드래그 이동, 버튼 배치 확인 |
| v0.2.0 | 스크롤 + 복사/붙여넣기 | 타 앱 포커스 유지하며 스크롤·복붙 동작 |
| v0.3.0 | 한/영 전환 | Windows·macOS·Linux 각 OS에서 전환 동작 |
| v0.4.0 | 설정 & UX 개선 | 위치 저장·복원, 스크롤 강도 조절, 툴팁, 우클릭 메뉴 |
| v1.0.0 | 릴리스 | 패키징, 문서 완성, 기본 CI |

---

## 6. 미결 사항 (Open Questions)

1. 한/영 전환 — macOS에서 `Ctrl+Space`가 Spotlight와 충돌할 수 있음. 대안 시뮬레이션 방법 검토 필요.
2. Wayland 환경에서 `pynput` 전역 이벤트 주입 제약 확인 필요.
3. 스크롤 1틱의 기본값(3틱) 및 사용자 설정 범위 확정 필요.
4. 빌드 배포 대상 플랫폼 우선순위 확정 (Windows 우선 여부).
